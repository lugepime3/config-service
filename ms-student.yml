server:
  port: 8084

custom:
  message: "Hola, este mensaje viene desde GitHub y el Config Server"

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true

# Esta parte de Eureka está bien que esté aquí
eureka:
  instance:
    statusPageUrlPath: /actuator/info
    healthCheckUrlPath: /actuator/health